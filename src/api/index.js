import axios from 'axios';

class API {
  static get(url, payload) { // eslint-disable-line no-unused-vars
    return axios.get(url);
  }

  static post(url, payload) { // eslint-disable-line no-unused-vars
    return axios.get(url);
  }
}

export default API;
